{
  "id": "grape",
  "name": "Grape",
  "category": "framework",
  "file_extension": "rb",
  "install_guide_url": "https://github.com/ruby-grape/grape",
  "presentation": { "accent": "#6b46c1" },
  "profile": "framework",
  "config_fields": [
    {
      "key": "ruby_path",
      "label": "Ruby binary",
      "field_type": "file_path",
      "required": true,
      "primary": true,
      "detect": { "commands": ["ruby"] }
    },
    {
      "key": "bundle_path",
      "label": "Bundler binary (skeleton install)",
      "field_type": "file_path",
      "required": false,
      "detect": { "commands": ["bundle"] }
    }
  ],
  "run": {
    "program": "{{ruby_path}}",
    "args": ["{{entry_file}}"]
  },
  "skeleton": {
    "bundled_dir": "grape",
    "sync_exclude_dirs": ["vendor", ".bundle"],
    "sync_exclude_files": [],
    "dependency_install": {
      "program": "{{bundle_path}}",
      "args": ["install", "--path", "vendor/bundle", "--without", "development", "test"],
      "vendor_marker": ".bundle/config",
      "manifest_files": ["Gemfile", "Gemfile.lock"]
    }
  },
  "prepare": {
    "output": "runspace_bootstrap.rb",
    "template": "bundle_bootstrap.tpl"
  },
  "terminal_env": {
    "framework_root": "RUNSPACE_FRAMEWORK_ROOT",
    "workspace": "RUNSPACE_WORKSPACE"
  },
  "version_probe": { "arg": "--version" }
}
