{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-execution",
    "allow-snippet",
    "allow-environment",
    "dialog:default",
    "opener:default"
  ]
}
